# Tastes
- Prefers incremental, staged development within a feature: populate the data/content first, then iterate on styling piece by piece in subsequent passes — explicitly framed the panel work as "populate it with [speed, status, signal strength] and then we will work on styling each piece of information" rather than asking for a finished, fully-styled result in one go. Confidence: 0.6
- Likes to understand how the current implementation works before making changes — asks the assistant to explain existing behavior (e.g., how the access token is obtained in screens/services) as a precursor to performing updates (also checks whether shared infrastructure like an API client wrapper already exists before adding new code). Frequently hands the assistant individual source files (e.g., `@.../tripHistory.service.ts`, `@.../TripDetailsScreen.tsx`) to review/cross-check against related code before any change is made. Confidence: 0.8
- Prefers focused, single-component changes — explicitly narrows a task's scope to one component or screen (e.g., "I need us to focus on the scrubber component"; "Let's work on the MapScreen once more") rather than bundling broad refactors. Confidence: 0.6
- Cares about code commenting conventions: when a commenting pattern caused a runtime warning (JSX `{/* */}` comments inside a native `MapView`), asked for the recommended approach for commenting code going forward rather than just accepting the one-off fix — wants to adopt a best-practice, compile-time-safe commenting style (e.g., `//` line comments, comments in JS context, self-documenting named components). Confidence: 0.5
- Prefers staying on the Expo-blessed, zero-native-config path for dependencies: explicitly chose to keep `@expo/vector-icons` over migrating to `react-native-vector-icons` (which would require config plugins and native font-linking), and when exploring alternatives prefers options that stay within the same package/ecosystem (e.g., MaterialIcons from `@expo/vector-icons`) over cross-library migrations. Confidence: 0.5
- Prefers conservative, minimal UI positioning changes: when one element overlaps another (e.g., the fullscreen button overlapping the playback controls bar), keep the element in its original location and compute the offset needed to clear the overlapping content (measured dynamically via `onLayout`) rather than relocating it elsewhere or hardcoding a fixed value — rejected moving the fullscreen button to the top-right as "too aggressive". Confidence: 0.7
- Prefers minimal, overflow-proof UI structures: when rendering one element per data point (e.g., a `TouchableOpacity` per trip path point) can overflow the available width, replace them with a single interactive element whose position is computed as a percentage of the container width (e.g., a track + thumb scrubber) rather than capping/shrinking the rendered items. Confidence: 0.7
- Prefers centralizing third-party library usage behind an app-owned typed wrapper/abstraction (e.g., an `AppIcon` component wrapping the icon set with names typed against the glyph map) so future icon swaps or reverts touch a single file — explicitly approved the assistant's wrapper-based migration plan ("Perform the migration to Material Design as you suggested"). Confidence: 0.5
- Refers to specific code elements by file line numbers when requesting changes (e.g., "the view subcomponent on lines 49 to 51... this is the element that displays the current bearing") rather than only by prop/component names. Confidence: 0.4
- Prefers promoting components to a general/shared level (e.g., `src/shared/components/`) as soon as they're needed by more than one feature, instead of keeping them feature-local or duplicating them — explicitly asked to move `VehicleMarker` out of `dashboard/components` because the same component would be reused on `TripDetailsScreen`, and later asked to place the shared `ZoomControls` on `TripDetailsScreen` right after it was extracted from `MapScreen`. Confidence: 0.9
- Cares about semantically correct anchoring of map markers: when the marker visual has a natural point (e.g., a map-pin icon), that point must sit exactly on the coordinate (bottom-center anchor) rather than being centered like a generic circle marker — explicitly asked to align the pin's bottom-center with the GPS location. Confidence: 0.5
- Prefers precise geometric alignment for rotating overlay elements: when a rotating element (e.g., a bearing arrow) must sit over another element (e.g., an icon badge), match the rotating element's parent container height to the element being overlaid and set `transformOrigin: 'center center'` so rotation pivots around the element's own center rather than orbiting the container's corner — explicitly proposed this exact fix ("match its parent's view height with the height of the view of the icon... set its transform origin to center center"). Confidence: 0.7
- Prefers intentional visual overflow for protruding indicators: when an indicator must point outward from an underlying element (e.g., a bearing arrow sticking up out of an icon badge), it should overflow its parent with its base aligned flush against the parent's outer edge (bottom-anchored holder + `justifyContent: 'flex-end'`) rather than being kept inside the container — explicitly requested "the bearing arrow should overflow its parent, its base must match the outer edge of its parent". Confidence: 0.6
- Proposes concrete implementation approaches and expects them to be carried out as specified (e.g., dictated the exact container-height and transform-origin fix for the bearing arrow, and required the polygon to sit on the parent's edge aligned with the icon background's outer edge) rather than leaving the fix open-ended for the assistant to decide. Confidence: 0.6
- Prefers scaling composite UI elements uniformly as a whole (e.g., "enlarge the entire marker element by 20%") rather than resizing individual sub-parts — expects one scale constant to drive all derived sizes so pin, badge, icon, and overlays grow proportionally together (asked "what is the formula for experimenting with the overall marker size?" and then agreed, "yes please", to deriving every sub-element — badge, icon, bearing arrow, border radius — from the single `MARKER_SCALE` knob instead of leaving any hardcoded sizes). Confidence: 0.8
- Notices and reports when a rendered element's visual bounds no longer match its container (e.g., "the main pin element overflows on the bottom area" after scaling; "every value on marker_scale above 0.7 causes the inner elements to overflow") — expects the fix to make the visual coincide exactly with the container edge (e.g., trimming trailing empty SVG viewBox space so the pin's tip is the actual bottom), while keeping `overflow: "visible"` only where overflow is intentional (like the protruding bearing arrow). Confidence: 0.65
- Notices and reports when a scaled/rendered element's visual bounds no longer match its container (e.g., "the main pin element overflows on the bottom area" after a 20% enlargement) — expects the fix to make the visual coincide exactly with the container edge (trimming trailing empty SVG viewBox space so the pin tip is the actual bottom), while keeping `overflow: "visible"` only where overflow is intentional (like the protruding bearing arrow). Confidence: 0.5
- Prefers scale-free SVG artwork: explicitly asked to convert an SVG `PIN_PATH` to relative points (0..1) so the path is resolution-independent and scales naturally with the container, replacing per-axis render-time scaling (`PIN_SCALE` multipliers) with a single `MARKER_SCALE` applied once to the base size — avoids maintaining parallel scale constants. Confidence: 0.6
- Reverts to a known-good previous version when an attempted fix becomes too convoluted or breaks more than it solves, rather than continuing to patch the broken approach (e.g., called the multi-step marker restructure "a mess" and reverted it outright; later, when a change broke two existing features — the live heading-arrow rotation refresh and the callout — asked "can we revert those updates?" and expected a clean, full revert including removal of now-dead props/imports/styles). Working, established functionality takes priority over retaining a half-working new feature. Confidence: 0.7
- Proposes their own root-cause hypotheses while debugging and expects the assistant to verify them against the actual code (e.g., "I believe the Marker element is the issue here. It has a fixed size") rather than being talked out of or around them. Confidence: 0.5
- Prefers simple, direct fixes over architectural restructures: when the assistant proposed redrawing the Material icon as an SVG path so the whole marker could live in one scalable `<Svg>`, the user rejected it and asked to "simply find a solution to resize the map element" — wants the existing structure (RN views + AppIcon badge) preserved and the marker element itself resized, rather than replacing working visuals to satisfy the scaling goal. Confidence: 0.6
- Prefers to park unresolved, research-heavy issues and return to them later rather than continuing to iterate in the same session (e.g., "I will return to this issue at a later point as it requires extra research") — expects the code to be left in a consistent, resumable state when the thread is paused. When pausing mid-debugging (e.g., "I don't have any more time for debugging this today"), explicitly asks for a file-by-file handoff — which files are involved in the process and what each does, plus the open threads and any leftover temporary diagnostics to remove — so the work can be documented and resumed at a later point. Confidence: 0.6
- Values production-code hygiene: when the assistant flags debug leftovers, dead code, and type escapes in existing code, agrees to a dedicated polish pass — removing `console.log` debugging lines, unused styles, stray blank lines, and dropping `as any` casts where the library's own types already cover the field (e.g., `request.codeVerifier` typed `string | undefined`) rather than leaving them in place. Confidence: 0.65
- Prefers reducing repeated-entry friction in forms: explicitly asked to persist the last successfully submitted org identifier (realm slug) and, when it exists, replace the text input with a tappable pill the user can simply select to re-submit — instead of re-typing the same value on every login. Expects the persisted value to survive full app close/reopen (cold start) on the emulator and reported the bug when the pill still failed to appear after a restart — again verified by closing and reopening the app and reporting that `savedOrgSlug` was lost on cold start. Continued to verify across restarts in later sessions (reported the pill appearing only momentarily in-session before being lost on close). Confidence: 0.8
- Prefers storing persisted identifiers/tokens in Keychain (`react-native-keychain`) rather than AsyncStorage: when asked to persist the org slug locally, accepted the assistant's decision to store it in the same Keychain entry as the tokens (per project guidance that tokens/identifiers must never go in AsyncStorage), with logout clearing it alongside the tokens. Confidence: 0.5
- Communicates the runtime environment/device context proactively when debugging platform-specific issues (e.g., "Note I am working on Android Studio emulator"; clarifying that `http://10.0.2.2:3000` is the local backend reachable from the emulator, "not open remote") so persistence/storage and networking debugging accounts for emulator/localhost behavior rather than assuming a physical device or remote service. Confidence: 0.5
- Expects persisted-state writes to preserve previously stored fields: when a fix required a newly persisted field (orgSlug) to survive later overwrites, applied the same "merge the previous stored entry into the new object" pattern to every write path that rewrites the persisted record (token exchange AND token refresh), so no later write silently drops a field written earlier. Confidence: 0.6
- Reports diagnostic results by pasting the exact console/log output verbatim (e.g., the `[loadTokens] parsed {...}` line; later pasted the full `[exchangeCodeForTokens]`/`[logout]` Metro logs with no commentary) rather than summarizing it, so the assistant interprets the raw data itself and continues the debug loop — also carries out the assistant's controlled test sequences (reload, clear app data, sign in fresh, sign out) and feeds back the raw output. Confidence: 0.7
- Cares about server-side session semantics in OIDC flows: when the app's Keycloak logout wasn't invalidating the browser session, the user cared whether the backend actually implements logout and wanted the flow explained (end-session endpoint vs. local token clearing) — expects the assistant to verify what the backend/service actually exposes and to clarify which service owns which step of the flow (reported again in a later session that "Signing out doesn't seem to trigger end_session properly" after the id_token_hint fix — the end-session behavior is re-verified against the real environment, not assumed fixed). Confidence: 0.5
- When a fix fails in their environment, expects a switch from speculation to verification: on "neither of the issues is resolved," the user's session shifted to instrumenting the actual code paths — reading the current token-exchange and logout code, probing the backend realm-resolution endpoint with curl, and adding temporary console.log diagnostics at the exact decision points (token-exchange response keys, id_token presence, orgSlug at exchange time, end-session URL/status) to get hard evidence instead of guessing at the cause. Confidence: 0.4
- Treats temporary debug instrumentation as something to be consciously tracked and removed once the issue is resolved: when pausing a debug session, accepted the assistant's note that `src/auth/useAuth.ts` "currently has temporary diagnostic console.logs ... that should be removed once both issues are confirmed resolved" as part of the handoff for resuming later — i.e., expects the paused-work summary to flag leftover diagnostics so they aren't forgotten in production code. Confidence: 0.5
- Prefers sourcing real user identity data from the identity provider instead of hardcoded mocks: explicitly asked to wire the drawer header's first/last name to the actual user name from OpenRemote (decoded from the stored OIDC `id_token` claims, `given_name`/`family_name`) rather than the mock `currentUser` — expects UI chrome to show authenticated user data that survives cold starts via persisted tokens. Confidence: 0.55
- Prefers real image assets (PNG) over icon-font glyphs for the app's logo/branding, and asks for the ecosystem's canonical storage convention rather than inventing one: for a login-screen logo, accepted the Expo-blessed top-level `assets/` directory (sibling of `src/`, covered by `assetBundlePatterns`, loaded via `require()`, no custom Metro config) with static assets kept out of `src/` and out of the generated `ios/`/`android/` folders. Confidence: 0.5
- Prefers styled, custom-rendered UI over native unstyled defaults when visual polish is wanted: for the map marker's infobox, asked whether the native callout `description` could accept markup ("is it possible to add markup on the description? I would like to provide some styling") — prompting a custom `<Callout>` with styled title/rows and colored values instead of settling for the plain OS bubble. Confidence: 0.5
- Prefers shared components to own presentation, driven by structured data props from call sites: when asked how to wire the styled callout, chose the "structured callout prop" design — the screen passes a `{ title, rows }` object (rows with optional per-value colors) and the shared `VehicleMarker` renders the styled `<Callout>` internally, falling back to native `title`/`description` when not provided — rather than having the screen build the markup as children. Confidence: 0.4
- Prefers visual data indicators over raw integer values for bounded metrics: explicitly asked to replace the signal-strength integer with a 5-bar signal meter (bar count = value, growing bar heights) rather than showing the number, and specified exact semantic color thresholds for the bars themselves (`< 3` red, `=== 3` orange, `> 3` green) instead of leaving the color mapping open-ended. Confidence: 0.5
- Prefers semantic theme color tokens for status indicators over generic status-to-color mappings: explicitly asked to switch the active `panelStatusDot` background to `colors.activeText` rather than the `markerColor(status)` mapping, keeping the generic mapping only as a fallback for the other statuses. Confidence: 0.5
- Prefers live, always-current data in the UI over static snapshots: when the vehicle panel values (speed/status/signal) were frozen because selection stored a snapshot of the vehicle, asked "Is it possible to offer live updates for those values?" — expects displayed values to stay in sync with the ongoing data refresh (polling) rather than freezing at selection time. Confidence: 0.6
- Prefers to plan and weigh alternative approaches before any code is written: opened the marker-styling task with "Let's plan on how to address the following issue. What approaches do we have…" — asks for a written plan of options (design choice, alternatives considered, files to change, verification steps) and only then proceeds to implementation, rather than requesting a fix directly. Confidence: 0.6
- Wants map assets to visually distinguish state at a glance: explicitly requested slightly different marker styling for inactive vehicle assets (greyed/off treatment) and for the actively selected asset (highlighted treatment), so selection and offline state are readable from the map without opening panels. For the selected state, prefers a high-contrast semantic color — success green (`colors.active`) for both the pin fill and the icon (not white) — over dark blue, because green pops more against the default blue pins. Confidence: 0.6
- Prefers extracting inline UI blocks into reusable, self-contained shared components under `src/shared/components/` even before a second consumer exists: explicitly asked to pull the map zoom controls out of `MapScreen` into its own file because "it will have to be reusable" — expects the component to own its styling and expose a small props/callback API (e.g., `onZoomIn`/`onZoomOut`) so other screens (like the trip-details map) could adopt it later. Also pre-created the target file scaffold (empty `ZoomControls.tsx`) and handed it to the assistant to implement. Confidence: 0.6
- Prefers shared components to encapsulate behavior (not just styling), so every consumer gets identical behavior from a single source of truth: after `ZoomControls` was used on two screens with duplicated `zoomBy` implementations, asked to "incorporate zoomBy function within the component... I would like consistency in its behaviour" — the shared component owns the zoom logic internally (accepting a `mapRef` prop plus a configurable `zoomFactor` default) instead of exposing per-screen callbacks. Confidence: 0.7
- Avoids scaling map markers: when the `selected` variant was given a scale-up transform (1.15), explicitly asked to remove it — "we need to avoid scaling because I have an issue with the map marker scale" — so selected/inactive states must be differentiated by color alone at a constant marker size rather than any size transform. Confidence: 0.8
: 0.8
